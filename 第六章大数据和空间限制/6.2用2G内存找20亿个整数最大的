题目描述：一个包含20亿全32位整数的大文件，在其中找出现次数最多的数，内存限制2GB。
词频统计的经典方法就是哈希表，通过key,value
在实际应用种要计算单个key,value大小，内存，总文件大小等来选择是否需要分成小文件或者分到多个机器上。
原理：根据哈希函数的性质，同一种数不可能被哈希到不同的小文件上，同时保证每个小文件的处理不会超过内存限制。

解法：将20亿数的大文件分到16个小文件中，然后对每个小文件通过哈希表统计每个数出现的次数，得到16个小文件各自出现次数最多的数及次数。
最后比较16个小文件次数最多的数，选出次数最多的。
